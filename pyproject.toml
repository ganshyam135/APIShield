[project]
name = "apishield"
version = "0.1.0"
description = "Backend-focused API management and observability platform built with FastAPI."
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.138.2",
    "uvicorn>=0.49.0",
]
